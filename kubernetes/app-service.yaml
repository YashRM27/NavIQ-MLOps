apiVersion: v1
kind: Service
metadata:
  name: naviq-service
spec:
  selector:
    app: naviq
  type: NodePort
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30080